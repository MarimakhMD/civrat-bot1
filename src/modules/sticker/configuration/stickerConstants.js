"use strict";

const StickerConfigKey = Object.freeze({
  LIMIT: "sticker_limit",
});

const STICKER_LIMIT_FREE = 5;

const StickerComponentId = Object.freeze({
  SECTION: "civrat:v1:sticker:section",
});

module.exports = { StickerConfigKey, StickerComponentId, STICKER_LIMIT_FREE };
