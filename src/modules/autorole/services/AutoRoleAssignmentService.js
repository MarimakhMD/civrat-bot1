"use strict";const {AutoRoleAssignmentReason:Reason}=require("./AutoRoleAssignmentReason");const {AutoRoleAssignmentSeverity:Severity}=require("./AutoRoleAssignmentSeverity");class AutoRoleAssignmentService{constructor({transport,logService=null}){this.transport=transport;this.logService=logService;}async assign({eligibility,assignmentContext}){const result=(assigned,code,severity,details={})=>Object.freeze({assigned,code,severity,guildId:assignmentContext?.guildId||null,memberId:assignmentContext?.memberId||eligibility?.memberId||null,roleId:assignmentContext?.roleId||eligibility?.roleId||null,details:Object.freeze({...details})});if(!eligibility||!assignmentContext)return result(false,Reason.INVALID_ASSIGNMENT_INPUT,Severity.ERROR);if(!eligibility.eligible){const skipped=result(false,Reason.ELIGIBILITY_REJECTED,Severity.SKIPPED,{eligibilityCode:eligibility.code});this.logService?.delivery({type:"AUTOROLE_SKIPPED",...skipped});return skipped;}try{await this.transport.assignRole(assignmentContext.member,assignmentContext.role);const success=result(true,Reason.NONE,Severity.NONE);this.logService?.delivery({type:"AUTOROLE_ASSIGNED",...success});return success;}catch(error){const failed=result(false,Reason.DISCORD_ASSIGNMENT_FAILED,Severity.ERROR);this.logService?.delivery({type:"AUTOROLE_ASSIGNMENT_FAILED",...failed});return failed;}}}module.exports={AutoRoleAssignmentService};
