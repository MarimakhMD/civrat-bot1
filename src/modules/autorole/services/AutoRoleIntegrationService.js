"use strict";
class AutoRoleIntegrationService{constructor({autoRoleService,eligibilityMapper,eligibilityService,assignmentService,logService}){this.autoRoleService=autoRoleService;this.eligibilityMapper=eligibilityMapper;this.eligibilityService=eligibilityService;this.assignmentService=assignmentService;this.logService=logService;}async handleMemberJoined({guildId,member,role,botCapability}){const config=await this.autoRoleService.read(guildId);const input=this.eligibilityMapper({member,role,bot:botCapability,config:{enabled:config.autorole_enabled,memberRoleId:config.autorole_member_role_id,botRoleId:config.autorole_bot_role_id}});const eligibility=this.eligibilityService.validate(input);const result=await this.assignmentService.assign({eligibility,assignmentContext:{guildId,memberId:member.id,roleId:role?.id||null,memberType:member.isBot?"bot":"member",member,role}});return result;}}
module.exports={AutoRoleIntegrationService};
