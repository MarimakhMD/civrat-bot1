"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {toggleAutoRole,selectAutoRole}=require("../interactions/configureAutoRole");const {AutoRoleComponentId:Id}=require("../configuration/autoRoleConstants");test("AutoRole routes persist and refresh localized panel",async()=>{let config={autorole_enabled:false};let updates=0;const c={guildId:"g",t:k=>k,service:{read:async()=>config,update:async(_g,p)=>config={...config,...p}},envelope:{transport:{update:async()=>updates++}}};await toggleAutoRole(c);await selectAutoRole({...c,envelope:{customId:Id.MEMBER_ROLE,values:["role"],transport:{update:async()=>updates++}}});assert.equal(config.autorole_enabled,true);assert.equal(config.autorole_member_role_id,"role");assert.equal(updates,2);});
