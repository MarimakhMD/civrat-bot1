"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {AutoRoleAssignmentService}=require("../services/AutoRoleAssignmentService");const {AutoRoleAssignmentReason:Reason}=require("../services/AutoRoleAssignmentReason");test("assignment assigns eligible and skips ineligible without transport",async()=>{let calls=0;const service=new AutoRoleAssignmentService({transport:{assignRole:async()=>calls++}});const context={guildId:"g",memberId:"m",roleId:"r"};const success=await service.assign({eligibility:{eligible:true,memberId:"m",roleId:"r"},assignmentContext:{...context,member:{roles:{add:async()=>{}}},role:{id:"r"}}});const skipped=await service.assign({eligibility:{eligible:false,code:"ROLE_TOO_HIGH",memberId:"m",roleId:"r"},assignmentContext:{...context,member:{roles:{add:async()=>{}}},role:{id:"r"}}});assert.equal(success.assigned,true);assert.equal(skipped.code,Reason.ELIGIBILITY_REJECTED);assert.equal(calls,1);});
