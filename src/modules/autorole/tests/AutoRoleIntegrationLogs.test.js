"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {AutoRoleAssignmentService}=require("../services/AutoRoleAssignmentService");
function input(eligible=true){return {eligibility:{eligible,code:eligible?null:"ROLE_TOO_HIGH",memberId:"m",roleId:"r"},assignmentContext:{guildId:"g",memberId:"m",roleId:"r",member:{roles:{add:async()=>{}}},role:{id:"r"}}};}
test("AutoRole logs assignment success, skip and transport failure in guild context",async()=>{const events=[];const logService={delivery:e=>events.push(e)};const success=new AutoRoleAssignmentService({transport:{assignRole:async()=>{}},logService});await success.assign(input());await success.assign(input(false));const failed=new AutoRoleAssignmentService({transport:{assignRole:async()=>{throw new Error("x")}},logService});await failed.assign(input());assert.deepEqual(events.map(e=>e.type),["AUTOROLE_ASSIGNED","AUTOROLE_SKIPPED","AUTOROLE_ASSIGNMENT_FAILED"]);assert.ok(events.every(e=>e.guildId==="g"));});
