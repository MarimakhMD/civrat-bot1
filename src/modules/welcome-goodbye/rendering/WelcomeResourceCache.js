"use strict";class WelcomeResourceCache{constructor({ttlMs=300000,maxEntries=100}={}){this.ttlMs=ttlMs;this.maxEntries=maxEntries;this.values=new Map();}get(key){const v=this.values.get(key);if(!v||Date.now()-v.at>this.ttlMs){this.values.delete(key);return null;}return v.value;}set(key,value){if(this.values.size>=this.maxEntries)this.values.delete(this.values.keys().next().value);this.values.set(key,{value,at:Date.now()});return value;}invalidate(key){this.values.delete(key);}clear(){this.values.clear();}}module.exports={WelcomeResourceCache};
