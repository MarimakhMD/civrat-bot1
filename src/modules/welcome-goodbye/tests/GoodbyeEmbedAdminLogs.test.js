"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {WelcomeAdminAction,WelcomeAdminLogService}=require("../services/WelcomeAdminLogService");test("Goodbye Embed administration events are stable",()=>{const events=[];const logs=new WelcomeAdminLogService({logger:{info:(_m,e)=>events.push(e)}});for(const action of [WelcomeAdminAction.GOODBYE_EMBED_ENABLED,WelcomeAdminAction.GOODBYE_EMBED_DISABLED,WelcomeAdminAction.GOODBYE_EMBED_COLOR_CHANGED])logs.record({action,guildId:"g",actorId:"u"});assert.equal(events.length,3);});
