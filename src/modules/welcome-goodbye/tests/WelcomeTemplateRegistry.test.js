"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {WelcomeTemplateRegistry}=require("../rendering/WelcomeTemplateRegistry");test("registry discovers three official templates and supports deterministic random selection",()=>{const r=new WelcomeTemplateRegistry();assert.equal(r.discover().length,3);assert.equal(r.select({mode:"fixed",templateId:"template-1"}).id,"template-1");assert.equal(r.select({mode:"random",templateIds:["template-2","template-3"],random:()=>.99}).id,"template-3");});
