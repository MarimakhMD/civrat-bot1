"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {openWelcomeMessageModal,submitWelcomeMessage}=require("../interactions/welcomeMessageModal");test("Welcome message modal persists submitted message",async()=>{let saved;const ctx={guildId:"g",t:k=>k,settings:{update:async(_g,p)=>saved=p},config:{welcome_message:"Old"},envelope:{transport:{showModal:async()=>{},update:async()=>{}},fields:{message:"New"}}};await openWelcomeMessageModal(ctx);await submitWelcomeMessage(ctx);assert.equal(saved.welcome_message,"New");});
