"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {toggleGoodbye,useWelcomeChannelForGoodbye}=require("../interactions/configureGoodbye");test("Goodbye actions persist and refresh localized panel",async()=>{let config={goodbye_enabled:false,welcome_channel_id:"w"};let updates=0;const c={guildId:"g",t:k=>k,settings:{get:async()=>config,update:async(_g,p)=>config={...config,...p}},envelope:{transport:{update:async()=>updates++}}};await toggleGoodbye(c);await useWelcomeChannelForGoodbye(c);assert.equal(config.goodbye_enabled,true);assert.equal(config.goodbye_channel_id,"w");assert.equal(updates,2);});
