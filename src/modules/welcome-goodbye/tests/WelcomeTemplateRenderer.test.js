"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {WelcomeTemplateRenderer,defaultPlaceholderProviders}=require("../services/WelcomeTemplateRenderer");test("renderer is extensible through registered providers",()=>{const r=new WelcomeTemplateRenderer({providers:defaultPlaceholderProviders()});r.register({name:"custom",resolve:()=>"ok"});assert.equal(r.render("{user} {custom}",{user:"@u"}),"@u ok");});
