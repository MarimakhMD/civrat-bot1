"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {toggleGoodbye,useWelcomeChannelForGoodbye}=require("../interactions/configureGoodbye");test("Goodbye configuration persists toggle and same channel",async()=>{let config={goodbye_enabled:false,welcome_channel_id:"welcome"};const ctx={guildId:"g",t:k=>k,settings:{get:async()=>config,update:async(_g,p)=>config={...config,...p}},envelope:{transport:{update:async()=>{}}}};await toggleGoodbye(ctx);await useWelcomeChannelForGoodbye(ctx);assert.equal(config.goodbye_enabled,true);assert.equal(config.goodbye_channel_id,"welcome");});
