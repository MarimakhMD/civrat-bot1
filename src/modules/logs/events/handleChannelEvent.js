"use strict";async function handleChannelEvent({channel,config,action,mapper,service,delivery}){if(!config.logs_enabled)return null;const entry=mapper.map({guildId:channel.guild.id,channelKey:"log_channel_update_channel_id",category:"channels",action,title:`logs.${action}`,details:{channelId:channel.id,channelName:channel.name}});return delivery.deliver({...entry,channelId:service.resolveDestination(entry,config)});}module.exports={handleChannelEvent};
