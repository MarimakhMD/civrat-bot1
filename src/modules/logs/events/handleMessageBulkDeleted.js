"use strict";async function handleMessageBulkDeleted({messages,config,mapper,service,delivery}){const first=messages.first();if(!config.logs_enabled||!first?.guild)return null;const entry=mapper.map({guildId:first.guild.id,channelKey:"log_message_delete_channel_id",category:"messages",action:"messages_bulk_deleted",title:"logs.messagesBulkDeleted",details:{count:messages.size}});return delivery.deliver({...entry,channelId:service.resolveDestination(entry,config)});}module.exports={handleMessageBulkDeleted};
