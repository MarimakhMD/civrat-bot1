"use strict";async function handleMemberJoined({member,config,mapper,service,delivery}){if(!config.logs_enabled||member.user.bot)return null;const entry=mapper.map({guildId:member.guild.id,channelKey:"log_member_join_channel_id",category:"members",action:"member_joined",title:"logs.memberJoined",details:{memberId:member.id}});return delivery.deliver({...entry,channelId:service.resolveDestination(entry,config)});}module.exports={handleMemberJoined};
