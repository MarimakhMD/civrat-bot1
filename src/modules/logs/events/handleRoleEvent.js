"use strict";async function handleRoleEvent({guild,config,action,roleId,memberId=null,mapper,service,delivery}){if(!config.logs_enabled)return null;const entry=mapper.map({guildId:guild.id,channelKey:"log_role_update_channel_id",category:"roles",action,title:`logs.${action}`,details:{roleId,memberId}});return delivery.deliver({...entry,channelId:service.resolveDestination(entry,config)});}module.exports={handleRoleEvent};
