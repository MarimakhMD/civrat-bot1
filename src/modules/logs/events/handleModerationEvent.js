"use strict";async function handleModerationEvent({guild,config,action,targetId,mapper,service,delivery}){if(!config.logs_enabled)return null;const entry=mapper.map({guildId:guild.id,channelKey:"log_moderation_channel_id",category:"moderation",action,title:`logs.${action}`,details:{targetId}});return delivery.deliver({...entry,channelId:service.resolveDestination(entry,config)});}module.exports={handleModerationEvent};
