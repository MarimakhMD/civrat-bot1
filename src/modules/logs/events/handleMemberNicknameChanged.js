"use strict";async function handleMemberNicknameChanged({oldMember,newMember,config,mapper,service,delivery}){if(!config.logs_enabled||oldMember.nickname===newMember.nickname)return null;const entry=mapper.map({guildId:newMember.guild.id,channelKey:"log_member_join_channel_id",category:"members",action:"member_nickname_changed",title:"logs.memberNicknameChanged",details:{memberId:newMember.id,oldNickname:oldMember.nickname,newNickname:newMember.nickname}});return delivery.deliver({...entry,channelId:service.resolveDestination(entry,config)});}module.exports={handleMemberNicknameChanged};
