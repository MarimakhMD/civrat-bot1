"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {toggleLogs}=require("../interactions/configureLogs");test("Logs toggle persists and refreshes localized view",async()=>{let config={logs_enabled:false};let updates=0;await toggleLogs({guildId:"g",t:k=>k,service:{read:async()=>config,update:async(_g,p)=>config={...config,...p}},envelope:{transport:{update:async()=>updates++}}});assert.equal(config.logs_enabled,true);assert.equal(updates,1);});
