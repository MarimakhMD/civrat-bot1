"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {LogsConfigService,LogsService,LogsEventMapper,LogsDeliveryService}=require("../index");test("Logs foundation remains resolver and transport neutral",async()=>{const config={logs_enabled:true};const service=new LogsConfigService({guildConfigResolver:{get:async()=>config,update:async(_g,p)=>({...config,...p})}});assert.equal((await service.read("g")).logs_enabled,true);const entry=new LogsEventMapper().map({channelKey:"log_moderation_channel_id",details:{x:1}});assert.equal(new LogsService().resolveDestination(entry,{}),null);assert.equal((await new LogsDeliveryService({transport:{}}).deliver(entry)).delivered,false);});
