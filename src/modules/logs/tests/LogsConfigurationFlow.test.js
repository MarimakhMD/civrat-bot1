"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {selectLogsCategory,selectLogsChannel}=require("../interactions/configureLogs");const {LogsComponentId:Id}=require("../configuration/logsConstants");const {LogsCategory}=require("../configuration/logsCategories");test("category selection routes channel persistence for all log categories",async()=>{for(const category of Object.values(LogsCategory)){let saved,view;const context={guildId:"g",t:k=>k,service:{update:async(_g,p)=>saved=p},envelope:{customId:`${Id.CHANNEL_PREFIX}:${category}`,values:["c"],transport:{update:async p=>view=p}}};await selectLogsCategory({...context,envelope:{values:[category],transport:{update:async p=>view=p}}});await selectLogsChannel(context);assert.ok(saved);assert.ok(view);}});
