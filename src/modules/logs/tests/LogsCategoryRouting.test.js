"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {LogsCategory,LogsCategoryChannelKey}=require("../configuration/logsCategories");const {selectLogsChannel}=require("../interactions/selectLogsChannel");test("all Free log categories map to one guild config key",async()=>{for(const category of Object.values(LogsCategory)){let saved;await selectLogsChannel({guildId:"g",envelope:{category,values:["c"]},service:{update:async(_g,p)=>saved=p}});assert.equal(saved[LogsCategoryChannelKey[category]],"c");}});
