"use strict";
const test=require("node:test"),assert=require("node:assert/strict");const {TicketTranscriptService}=require("../services/TicketTranscriptService");
test("transcript preserves legacy chronological format and fallback",()=>{const service=new TicketTranscriptService();assert.equal(service.build([{timestamp:"2025-01-02T00:00:00.000Z",author:"later",content:"b"},{timestamp:"2025-01-01T00:00:00.000Z",author:"first",content:""}]),"[2025-01-01T00:00:00.000Z] first: [pièce jointe / embed]\n[2025-01-02T00:00:00.000Z] later: b");assert.equal(service.build([]),"Aucun message dans ce ticket.");});
test("transcript is non-blocking for failures",async()=>{const service=new TicketTranscriptService();assert.equal((await service.deliver({channelId:"c",logChannelId:null,transport:{}})).code,"TRANSCRIPT_DESTINATION_MISSING");assert.equal((await service.deliver({channelId:"c",logChannelId:"l",transport:{fetchTranscriptMessages:async()=>{throw new Error("fail");}}})).code,"TRANSCRIPT_FAILED");});
