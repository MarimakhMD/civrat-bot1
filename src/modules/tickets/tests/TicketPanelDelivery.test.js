"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {TicketPanelDeliveryService}=require("../services/TicketPanelDeliveryService");const view={title:"t",content:"d",components:[{customId:"civrat:v1:tickets:create",label:"create"}]};test("ticket panel delivery sends once only when ready",async()=>{let calls=0;const service=new TicketPanelDeliveryService({panelService:{build:async()=>({ready:true,channelId:"c",view})},transport:{sendPanel:async()=>calls++}});assert.equal((await service.deliver("g",k=>k)).delivered,true);assert.equal(calls,1);});test("ticket panel delivery skips incomplete configuration",async()=>{let calls=0;const service=new TicketPanelDeliveryService({panelService:{build:async()=>({ready:false,code:"TICKET_CONFIG_INCOMPLETE"})},transport:{sendPanel:async()=>calls++}});assert.equal((await service.deliver("g",k=>k)).delivered,false);assert.equal(calls,0);});
