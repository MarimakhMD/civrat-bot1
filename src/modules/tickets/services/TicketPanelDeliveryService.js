"use strict";class TicketPanelDeliveryService{constructor({panelService,transport}){this.panelService=panelService;this.transport=transport;}async deliver(guildId,t){const panel=await this.panelService.build(guildId,t);if(!panel.ready)return {delivered:false,code:panel.code,details:{}};try{await this.transport.sendPanel(panel.channelId,panel.view);return {delivered:true,code:null,channelId:panel.channelId,details:{}};}catch(error){return {delivered:false,code:"TRANSPORT_ERROR",details:{}};}}}module.exports={TicketPanelDeliveryService};
