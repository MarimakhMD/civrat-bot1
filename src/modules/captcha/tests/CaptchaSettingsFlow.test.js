"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {toggleCaptcha,selectCaptcha}=require("../interactions/configureCaptcha");const {CaptchaComponentId:Id}=require("../configuration/captchaConstants");test("Captcha settings persist toggle channel and role",async()=>{let config={captcha_enabled:false};let updates=0;const c={guildId:"g",t:k=>k,service:{read:async()=>config,update:async(_g,p)=>config={...config,...p}},envelope:{transport:{update:async()=>updates++}}};await toggleCaptcha(c);await selectCaptcha({...c,envelope:{customId:Id.CHANNEL,values:["c"],transport:{update:async()=>updates++}}});await selectCaptcha({...c,envelope:{customId:Id.ROLE,values:["r"],transport:{update:async()=>updates++}}});assert.deepEqual(config,{captcha_enabled:true,captcha_channel_id:"c",captcha_role_id:"r"});assert.equal(updates,3);});
