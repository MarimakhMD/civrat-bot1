"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {createCaptchaRuntime}=require("../runtime/createCaptchaRuntime");test("member join reminder uses new runtime once and skips bots",async()=>{const runtime=createCaptchaRuntime({guildConfigResolver:{get:async()=>({captcha_enabled:false})}});const bot={user:{bot:true}};assert.equal((await runtime.handleMemberJoined(bot)).code,"BOT_MEMBER");});
