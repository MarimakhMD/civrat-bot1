"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {CaptchaConfigService}=require("../services/CaptchaConfigService");test("Captcha configuration reads and writes through resolver",async()=>{let config={captcha_enabled:false};const service=new CaptchaConfigService({guildConfigResolver:{get:async()=>config,update:async(_g,p)=>config={...config,...p}}});assert.equal((await service.read("g")).captcha_enabled,false);await service.update("g",{captcha_enabled:true,captcha_channel_id:"c",captcha_role_id:"r"});assert.deepEqual(config,{captcha_enabled:true,captcha_channel_id:"c",captcha_role_id:"r"});});
