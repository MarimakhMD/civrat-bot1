"use strict";

const AnalyticsConfigKey = Object.freeze({
  ENABLED: "analytics_enabled",
});

const ANALYTICS_DEFAULTS = Object.freeze({
  analytics_enabled: false,
});

module.exports = { AnalyticsConfigKey, ANALYTICS_DEFAULTS };
