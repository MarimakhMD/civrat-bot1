"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {InteractionRegistry,InteractionRouter,InteractionKind}=require("../../../core/interactions");test("unknown sanction route produces one error without side effects",async()=>{let replies=0,services=0;const router=new InteractionRouter({registry:new InteractionRegistry(),contextFactory:{create:async envelope=>({envelope,respondError:async()=>envelope.transport.replyError(),permissions:{require:async()=>services++}})}});await router.handle({kind:InteractionKind.COMMAND,name:"unknown",transport:{replyError:async()=>replies++}});assert.equal(services,0);assert.equal(replies,1);});
