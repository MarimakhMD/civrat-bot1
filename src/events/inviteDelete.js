const guildConfigService=require("../services/guildConfig");const inviteService=require("../services/inviteService");const {getLogsRuntime}=require("../modules/logs/runtime/getLogsRuntime");module.exports={name:"inviteDelete",once:false,async execute(invite){const invites=await invite.guild.invites.fetch().catch(()=>null);if(invites)inviteService.cacheGuildInvites(invite.guild.id,invites);const config=await guildConfigService.getGuildConfig(invite.guild.id);await getLogsRuntime().handleInviteEvent({guild:invite.guild,config,action:"invite_deleted",inviteCode:invite.code});}};
