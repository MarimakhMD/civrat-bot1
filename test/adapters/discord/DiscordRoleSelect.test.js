"use strict";const test=require("node:test"),assert=require("node:assert/strict");const {componentToRow}=require("../../../src/adapters/discord/DiscordResponseTransport");test("role-select view converts through existing Discord transport",()=>{const row=componentToRow({type:"role-select",customId:"civrat:v1:autorole:member-role",placeholder:"Role"});assert.equal(row.components[0].data.custom_id,"civrat:v1:autorole:member-role");});
