"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");
test("legacy member event responsibilities remain after Welcome runtime delegation",()=>{const added=fs.readFileSync("src/events/guildMemberAdd.js","utf8");const removed=fs.readFileSync("src/events/guildMemberRemove.js","utf8");for(const symbol of ["getAutoRoleRuntime","getCaptchaRuntime","handleInviteTracking","getSecurityRuntime","getWelcomeGoodbyeRuntime"])assert.match(added,new RegExp(symbol.replace(/[.]/g,"\\.")));for(const symbol of ["handleKickDetection","handleInviteDecrement"])assert.match(removed,new RegExp(symbol));assert.match(added,/getSecurityRuntime/);assert.doesNotMatch(added,/securityService\.recordRaidJoin/);});
