"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");test("guildMemberAdd delegates AutoRole and retains legacy module responsibilities",()=>{const source=fs.readFileSync("src/events/guildMemberAdd.js","utf8");assert.match(source,/getAutoRoleRuntime/);for(const symbol of ["getCaptchaRuntime","handleInviteTracking","getSecurityRuntime","getWelcomeGoodbyeRuntime"])assert.match(source,new RegExp(symbol.replace(/[.]/g,"\\.")));assert.doesNotMatch(source,/handleAutoRole/);assert.doesNotMatch(source,/securityService\.recordRaidJoin/);});
